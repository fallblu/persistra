# Persistra

Persistra v3 is a local-first Python library for point-in-time market research, strategy
development, and event-driven backtesting.

The documentation set is being rebuilt. Until the new structure lands, the
[README](https://github.com/fallblu/persistra) records the supported commands, and the
implementation itself is the reference for the public API reached through
`Project.services`.
